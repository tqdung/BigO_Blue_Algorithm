#include <iostream>

using namespace std;

int binarySearchFirst(int[] arr, int left, int right, int value)
{
    int mid;
    // while (left <= right)
    // {
    //     mid = left + (right - left) / 2;
    //     if (arr[mid] == value && mid == left || arr[mid - 1] != value)
    //     {
    //         return mid;
    //     }
    //     else if (arr[mid] < x)
    //     {
    //         left = mid + 1;
    //     }
    //     else
    //         right = mid - 1;
    // }
    return mid;
}

int main()
{
    while (true)
    {
        /* code */
        int n, q;
        cin >> n >> q;
        if (n == 0 || q == 0)
            break;
    }

    return 0;
}